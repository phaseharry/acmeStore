import axios from 'axios'

//action types


//action creators


//thunks

const reducerAuth = (state = {}, action) => {
    switch(action.type){
        default: 
            return state
    }
} 

export default reducerAuth